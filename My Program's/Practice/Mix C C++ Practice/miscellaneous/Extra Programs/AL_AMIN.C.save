#include<stdio.h>
int main()
{
    int ar[100],i,j,item=4,k=3,p;
    printf("Enter input array number is:");
    scanf("%d",&p);
    printf("dshgfdu:");
    for(i=0;i<=p;i++)
    {
        scanf("%d",&ar[i]);
    }
     for(i=0;i<=p;i++)
      {
        for(i=k;i<=p-1;i++)
         {
           ar[i]=ar[i+1];
         }
        if(item=ar[k])
           {
               break;
           }
      }
    for(j=0;j<=p;j++)
        {
         printf("%d ",ar[j]);
        }
    return 0;
}

