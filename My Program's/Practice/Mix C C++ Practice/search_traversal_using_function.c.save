#include<stdio.h>
void search(int LA[],int N,int LB,int item)
{
    int i;
    for(i=LB; i<N; i++)
    {
        if(item==LA[i])
        {
            printf("Item found in position:%d\n",i+1);
            //break;
        }
    }
}
 main()
{
    int LB=0,LA,N=5,item=2;

    int i;
    int array_data[10000];
    printf("Enter the value's of array:");

    for(i=0;i<N;i++)
    {
      scanf("%d",&array_data[i]);
    }
    search(array_data,N,LB,item);
}
